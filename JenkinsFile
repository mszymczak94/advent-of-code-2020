pipeline {
	agent any
	stages {
		stage('Build Application') {
			steps {
				sh 'mvn clean package'
			}
			steps {
				postBuildPublishers {
					echo "Now Archiving the Artifacts...."
					archiveArtifacts pattern: '**/*.war'
				}
			}
		}
	}
}